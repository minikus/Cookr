<div class="main">
  <h1>Chefs</h1>
  <% @users.each do |user| %>
    <div class="thumb">
      <%=link_to (image_tag user.image), user %>
      <%= link_to (user.first_name), user%>
    </div>
  <% end %>
</div>
