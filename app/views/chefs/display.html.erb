<div class="main">
  <h1>Chefs</h1>
  <% @users.each do |user| %>
  <div class="displayChefs">
      <div class="thumb">
      <%=link_to (image_tag user.image), user %>
      <h2><%= link_to (user.first_name), user%>
    </div>
  </div>

  <% end %>
</div>
